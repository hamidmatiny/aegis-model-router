# Starting backlog — day one

- First audit: provider package tests vs README gaps (grok-build-0.1 not in fallback; limited /v1/providers model_status tests; env override only OLLAMA_BASE_URL).
- Stale README: "Gateway orchestration is future work" contradicts live gateway pipeline — docs PR.
- Auth/error path hardening: embeddings_error + auth_errors already tested — confirm production key fingerprint reporting still honest.
